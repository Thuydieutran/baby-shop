{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="mt-3 btn btn-secondary text-light col-lg-4 offset-lg-4 col-6 offset-3">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
